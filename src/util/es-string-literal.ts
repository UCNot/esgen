/**
 * Creates JavaScript string literal.
 *
 * Encloses the string into quotes and properly {@link esEscapeString escapes it}.
 *
 * @param value - A string contained inside literal.
 * @param quote - Quote symbol to use.
 *
 * @returns String literal.
 */
export { jsStringLiteral as esStringLiteral } from 'httongue';
